{
  "identifier": "default",
  "description": "Default capability for all app windows (IPC, window controls, updater, process)",
  "windows": ["*"],
  "permissions": [
    "core:default",
    "updater:default",
    "process:allow-restart",
    "process:allow-exit"
  ]
}
